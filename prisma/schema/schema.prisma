generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["prismaSchemaFolder", "fullTextSearch"]
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}
